[storage]
type = "S3"
bucket = "${TEST_BUCKET}"
prefix = "test_data"

[job]
num_generations = 10
terminate_at_target_gen = false
num_replicates = 10
num_particles = 1000

[algorithm]
tolerance_descent_percentile = 75
max_num_failures = 50

[run]
base_config_path = "resources/base_config.json"
